#pragma once

#include <vector>

namespace SSTabSim
{
    using std::vector;

    /**
     * @brief Tableaux representation
     * @note 0 expressing empty square
     */
    using TableauxShape = vector<vector<int>>;
}