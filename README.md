# SSTabSimulator

Semistandard Tableaux Simulator

## 解説動画について

メールで送ったので消しておきました

## build

`mkdir build` is required only the first time.

```
cd src
mkdir build
cd build
cmake ..
cmake --build .
```

## execute

```
./SSTabSimulator
```
